# xychart

Keyword: `xychart-beta`.
Invariant: axes plus `bar` / `line` series.

## minimal

```mermaid
xychart-beta
  title Sales
  x-axis [Q1, Q2, Q3, Q4]
  y-axis "Units" 0 --> 100
  bar [20, 40, 60, 50]
```
