# @mermaid-js/layout-elk (vendored **source**)

TypeScript sources from the mermaid monorepo tag pinned with `vendor/mermaid`
(`mermaid@11.6.0` → package version **0.1.7**).

- Not npm `dist/` and not decompiled — see `LAYOUT_ELK_SRC_PIN`
- Built by `scripts/layout-elk-build/build.mjs` → `assets/vendor/layout-elk.min.js`
